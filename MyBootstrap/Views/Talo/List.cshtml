@model IEnumerable<MyBootstrap.Database.LocatedTaloViewModel>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TaloId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NykyLampo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TavoiteLampo)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TaloId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NykyLampo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TavoiteLampo)
            </td>

            @*<td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.Id })
                </td>*@
        </tr>
    }

</table>